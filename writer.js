const books={building:"building",understanding:"understanding",remembering:"remembering",reciting:"reciting",reading:"reading",making:"making",cooking:"cooking",becoming:"becoming"};
const form=document.getElementById("writerForm"),book=document.getElementById("book"),title=document.getElementById("title"),date=document.getElementById("date"),type=document.getElementById("type"),summary=document.getElementById("summary"),language=document.getElementById("language"),image=document.getElementById("image"),body=document.getElementById("body"),instruction=document.getElementById("instruction");
date.value=new Date().toISOString().slice(0,10);book.value=new URLSearchParams(location.search).get("book")||"building";
const safe=value=>value.replace(/\r?\n/g," ").replace(/"/g,"'").trim();
const slug=value=>value.toLowerCase().normalize("NFKD").replace(/[^\p{L}\p{N}]+/gu,"-").replace(/^-|-$/g,"")||`page-${Date.now()}`;
document.getElementById("template").addEventListener("click",()=>{const templates={cooking:"## The story\n\nWhy this food matters to me…\n\n## Ingredients\n\n- Ingredient and quantity\n\n## Method\n\n1. First step\n2. Next step\n\n## Serving note\n\nHow I like to serve it…",reciting:"## কবিতার কথা\n\nএই কবিতাটি কেন আমার কাছে গুরুত্বপূর্ণ…\n\n## আবৃত্তির অনুভব\n\nকণ্ঠ, ছন্দ ও অনুভূতির কথা…\n\n## শেখার জায়গা\n\nআমি এখনো যা শিখছি…"};body.value=templates[book.value]||"## The beginning\n\nWhat happened, or what I was thinking…\n\n## What I noticed\n\nThe idea I want to explore…\n\n## What stays with me\n\nThe lesson, question, or unfinished thought…";body.focus()});
form.addEventListener("submit",event=>{event.preventDefault();const filename=`${slug(title.value)}.md`;const front=["---",`title: "${safe(title.value)}"`,`date: ${date.value}`,`type: "${safe(type.value||"Journal page")}"`,`summary: "${safe(summary.value)}"`,`language: ${language.value}`,image.value?`image: ${safe(image.value)}`:"",image.value?`imageAlt: "${safe(title.value)}"`:"","published: true","---","",body.value.trim(),""].filter((line,index,array)=>line!==""||array[index-1]!=="").join("\n");const blob=new Blob([front],{type:"text/markdown;charset=utf-8"}),link=document.createElement("a");link.href=URL.createObjectURL(blob);link.download=filename;link.click();URL.revokeObjectURL(link.href);instruction.textContent=`Downloaded ${filename}. Upload it into content/${books[book.value]}/ on GitHub, then commit.`});
